---
title: Tags
kind: archives
---

<ul>
  <% site.blog.tags.sort_by(&:first).each do |tag, page| %>
    <li><a href="<%= page.path %>"><%= render_tag(tag) %> (<%= page[:page_items].size %>)</a></li>
  <% end %>
</ul>

